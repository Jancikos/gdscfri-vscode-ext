export class Village {
    code: string;
    name: string;
    type: string;

    constructor(code: string, name: string, type: string) {
        this.code = code;
        this.name = name;
        this.type = type;
    }
}